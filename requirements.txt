fastapi==0.115.6
uvicorn[standard]==0.34.0
google-genai==1.5.0
anthropic>=0.39.0
python-dotenv==1.0.1
psutil==6.1.1
# pyautogui wurde am 2026-07-31 ENTFERNT: es war nie importiert (die
# Desktop-Steuerung laeuft ueber xdotool als Unterprozess, siehe
# backend/tools/desktop.py), zog aber drei GPL-Pakete in den Backend-Prozess
# (MouseInfo/PyMsgBox GPL-3.0+, python3-xlib GPL-2.0) – unvereinbar mit der
# eigenen Apache-2.0. Nicht wieder aufnehmen, ohne das zu pruefen.
pillow==11.1.0
pytesseract==0.3.13   # Bild-OCR fuer Wissensbasis; benoetigt System-Paket: tesseract-ocr (+ tesseract-ocr-deu/-eng)
pdf2image==1.17.0     # OCR-Fallback fuer gescannte PDFs (rendert Seiten zu Bildern); benoetigt System-Paket: poppler-utils
python-multipart==0.0.20
websockets==14.2
python-pam>=2.0.2
six>=1.16.0
pdfplumber>=0.10.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23
edge-tts>=7.2.7        # TTS im Chat: Stimmen-Liste (/api/tts/voices) + Sprachsynthese; ohne edge-tts nur eine "Standard"-Stimme
wsgidav>=4.3.0
# ── Am 2026-08-12 nachgetragen: bis dahin NICHT deklariert, obwohl im Backend
# direkt importiert. Sie waren nur als Beigabe anderer Pakete vorhanden und
# haetten bei jeder venv-Bereinigung verschwinden koennen – genau so ist der
# PDF-Anhang auf ECHT gestorben (pypdf, siehe tests/test_pdf_attachment.py).
# BEWUSST OHNE Versionsangabe: die Pakete sind auf allen Servern vorhanden, eine
# Untergrenze koennte dort ein ungewolltes Upgrade ausloesen. Es geht um das
# VORHANDENSEIN, nicht um eine Version.
requests               # jira_client, sap_client, google_auth, web_extractor, ibs
httpx                  # knowledge_sync (Standort-Sync), model_caps, file_watcher, web_extractor
cryptography           # backend/license.py: Ed25519-Pruefung der Lizenz
starlette              # direkt genutzt in main.py (kommt sonst nur ueber fastapi)
# NICHT deklariert, mit Absicht: torch. Es kommt mit sentence-transformers, und
# ein eigener Eintrag hier wuerde die CUDA-Variante zurueckholen, die am
# 2026-07-19 auf ECHT bewusst gegen torch+cpu getauscht wurde (~4,6 GB).
# face-recognition + opencv gehoeren zum Vision-Skill und werden bei dessen
# Aktivierung installiert (skills/vision/skill.json → dependencies)
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0,<4.0
numpy<2.1
# WICHTIG – torch VOR dieser Datei aus dem CPU-Index installieren:
#   pip install --index-url https://download.pytorch.org/whl/cpu "torch==2.10.0+cpu"
# sentence-transformers zieht sonst das CUDA-Rad und damit 18 proprietaere
# NVIDIA-Pakete (~6 GB) auf eine VM OHNE GPU – torch.cuda.is_available() ist hier
# False, sie werden nie geladen. Nachgemessen 2026-07-31: venv 8,4 GB -> 2,4 GB.
# Die Einbettungen sind bitgleich (gleicher SHA ueber den Vektor), ein Reindex ist
# nach dem Wechsel also NICHT noetig.
# Die Versionsangabe muss das lokale Suffix "+cpu" tragen: mit "torch==2.10.0"
# haelt pip das bereits installierte CUDA-Rad fuer passend und tut nichts.
mcp>=1.5.0,<1.26
sse-starlette>=3.0.0,<3.1
ldap3>=2.9
APScheduler>=3.10,<4.0
watchdog>=3.0
pyotp>=2.9.0
qrcode[pil]>=7.4.2
